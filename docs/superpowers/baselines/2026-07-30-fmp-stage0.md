# Find My Pet Stage 0 baseline — 2026-07-30

Reference design: [`2026-07-30-fmp-full-site-design-system-design.md`](../specs/2026-07-30-fmp-full-site-design-system-design.md).

This is an observation-only baseline recorded before implementation of the approved
design foundation and home slice.  The product-code changes listed below were
already present in the worktree; this document does not attribute them to that
design plan.

## Dirty-worktree boundary observed before the plan

Modified paths:

- `package.json`
- `public/llms.txt`
- `scripts/indexnow.mjs`
- `src/app/(route)/abandonment/[detail]/page.tsx`
- `src/app/(route)/profile/page.tsx`
- `src/app/_components/AbandonmentCard.tsx`
- `src/app/_components/AbandonmentPagination.tsx`
- `src/app/_components/LostPagination.tsx`
- `src/app/_components/abandonment/ClosedNoticeBanner.tsx`
- `src/app/_components/main/AbandonmentList.tsx`
- `src/app/_components/main/LostList.tsx`
- `src/app/_components/ui/pagination.tsx`
- `src/app/faq/page.tsx`
- `src/app/page.tsx`
- `src/app/privacy/page.tsx`
- `src/app/search/page.tsx`
- `src/app/sitemap.ts`
- `src/app/terms/page.tsx`
- `src/lib/abandonment.ts`
- `src/lib/auth.ts`
- `src/lib/region.ts`

Untracked paths:

- `.superpowers/`
- `scripts/abandonment-status-contract.test.mjs`
- `scripts/category-contract.test.mjs`
- `scripts/test-utils/`
- `src/app/(route)/abandonment/page.tsx`
- `src/app/(route)/lost/page.tsx`
- `src/app/_components/category/`
- `src/app/_components/profile/WithdrawSection.tsx`
- `src/lib/pagination.ts`
- `src/lib/purposeCategories.ts`

## Executable baseline

| Command | Result | Notes |
| --- | --- | --- |
| `corepack yarn test:category` | pass (9/9) | Category, list-state, pagination, and copy contracts passed. |
| `corepack yarn tsc --noEmit` | pass | Exit 0. |
| `corepack yarn lint` | pass | Exit 0; five known warnings: three `react-hooks/exhaustive-deps` warnings (`LostDetailClient`, `FlyerManagementSection`, `SightingSection`) and two `@next/next/no-img-element` warnings (`SimilarCandidatesSection`, `register/page.tsx`). |
| `corepack yarn build` | pass | Exit 0; retains the same five lint warnings. Static generation logged DNS `ENOTFOUND fmp.platformholder.site` fetch failures in this sandbox, but completed all 21 pages successfully. |

Yarn also warned that its normal cache/global locations are not writable and used
the sandbox temporary cache.  These did not affect command success.

## Visual capture attempt

The app was started with its existing public API configuration at
`http://127.0.0.1:3000`.  A browser-control runtime was unavailable in the
capture environment (zero available browser backends), so no truthful 1440×1200
or 390×844 rendered screenshots could be produced.  No placeholder or mock
images were substituted.

The following paths were requested for visual capture but remain **unverified**:

- `/` (home)
- `/lost`
- `/abandonment`
- `/register`
- `/profile`

The server returned HTTP 200 for `/`, `/lost`, `/abandonment`, and `/register`
during the attempted local check.  Rendering and API/auth state could not be
visually inspected.  No real public lost or protection-detail identifier was
available; detail appearance is therefore also **unverified**.  The public API
host is not DNS-resolvable from this sandbox during static generation.

## Deferred completion condition

Capture the ten requested desktop/mobile images in an environment with an
available browser and the existing local API configuration.  Record the actual
rendered API/login/error state; do not replace it with mock content.
