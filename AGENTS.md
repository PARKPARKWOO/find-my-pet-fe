# Find-My-Pet 프론트엔드 작업 지침

- 제품 계약은 `../prd/find-my-pet/`와 `../animal/` API를 함께 확인한다.
- 인증의 SSOT는 HttpOnly 쿠키와 게이트웨이 자동 회전이다. 프론트에서 토큰을 읽거나 저장하거나 Authorization 헤더를 붙이거나 자체 reissue를 구현하지 않는다.
- 모든 API 호출은 `src/lib/api.ts`의 `withCredentials` 클라이언트를 재사용한다. 로그인 여부는 사용자 조회 API의 성공·실패로 판정하고 로그아웃은 서버 revoke 흐름을 사용한다.
- 인증 쿼리나 콜백 값은 세션 토큰 저장소로 사용하지 말고 필요한 전환 정보만 최소한으로 처리한다.
- 사용자 위치, 연락처, 반려동물 정보는 화면·로그·분석 이벤트에 불필요하게 노출하지 않는다.
- API 응답 모델, 검색 반경 규칙, SEO 메타데이터를 바꾸면 백엔드/PRD 계약과 오류·빈 상태를 함께 검증한다.
- push나 배포 전에는 현재 remote와 배포 연결 대상을 확인하되 계정 식별자를 문서에 고정하지 않는다.

## 검증

- lockfile에 맞춰 `yarn install --frozen-lockfile`을 사용한다.
- 변경 후 `yarn lint`와 `yarn build`를 실행하고 로그인·로그아웃·보호 화면·주요 검색 흐름을 확인한다.
- 호스팅 배포와 실제 OAuth 계정 설정은 사용자 확인 없이 실행하지 않는다.
